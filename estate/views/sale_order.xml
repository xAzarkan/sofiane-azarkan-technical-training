<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_sale_order_training_wizard" model="ir.actions.act_window">
            <field name="name">Training Wizard</field>
            <field name="res_model">training.wizard</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_training_wizard_form"/>
            <field name="target">new</field>
        </record>

        <record model="ir.ui.view" id="sale_order_view_inherit">
            <field name="name">sale.order.view.inherit1</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="/form/sheet/notebook/page/field[@name='order_line']/tree/field[@name='tax_id']" position="after">
                    <field name="training_date"/>
                    <field name="employee_id" widget="many2one"/>
                </xpath>

                <!-- Add the button after the confirm button -->
                <xpath expr="//button[@name='action_confirm']" position="after">
                    <button string="Request Approval" type="object" name="request_approval"/>
                    <button string="Select the training date" type="object" name="action_sale_order_training_wizard"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>

